# Single read-write root for now. Phase 5 replaces this with read-only A/B
# roots plus a data partition (docs/components/base-image.md).
[Partition]
Type=root
Format=ext4
CopyFiles=/
SizeMinBytes=3G
SizeMaxBytes=3G
